// Mock data lives in the entity API — re-exported here for backward compat
export { MOCK_EVENTS } from '@entities/Event/api/event.api';
export type { EventModel as MockEvent } from '@entities/Event';
