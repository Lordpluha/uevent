// Mock data lives in the entity API — re-exported here for backward compat
export { MOCK_ORGS, MOCK_MY_ORG } from '@entities/Organization/api/organization.api';
export type { Organization as MockOrg } from '@entities/Organization';
