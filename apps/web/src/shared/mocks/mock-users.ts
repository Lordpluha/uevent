// Mock data lives in the entity API — re-exported here for backward compat
export { MOCK_USERS, MOCK_CURRENT_USER } from '@entities/User/api/user.api';
export type { User as MockUser } from '@entities/User';
